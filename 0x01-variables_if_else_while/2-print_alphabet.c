#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a-z[26] = "abcdefghijklmnopqrstuvwxyz";
	int q;

	for (q = 0; q < 26; q++)
	{
		putchar(a-z[q]);

	}
	putchar('\n');
	return (0);
}

